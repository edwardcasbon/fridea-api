var Authenticate = {

    // Create token
    // Look up the app record using supplied API key and secret. If record exists
    // then create a token based on their app record ID and the app auth secret.
    createToken: function(apikey, secret, appSecret) {
    },

    // Verify token
    verifyToken: function(token, appSecret) {
    }

};

module.exports = Authenticate;
